# Tekniik — Architecture

Production Vite + React SPA. **Redesigned routes run the "Deep Ink" brand themes** (`src/styles/theme-ink.css`): brand navy `#202E5D` + teal `#72CCD6`, Satoshi display / Inter body, two modes toggled from the Nav — `data-theme="ink-light"` (default, cool paper) and `data-theme="ink"` (navy-black). **Every route is inked** (2026-07-20 — the case-study redesign inked the last base-theme pages, so the old `INK_ROUTES` gate in `App.jsx` is gone; the theme attribute is set unconditionally); mode persists as `localStorage['tekniik-ink-mode']` with values **`'dark'`/`'light'`**. Recurring ink devices: node-dot + mono eyebrow meta bars, `NN / EYEBROW` section heads, hairline ledgers, ghost stroke numerals, scroll-drawn teal rails, brand-navy `--band` sections with re-scoped semantic tokens, and slim `MiniCta` interstitial strips. The earlier "Bold Editorial" indigo descriptions below predate this and are being replaced section by section as pages are re-documented. Source of truth for copy: `references/tekniik-prototype-v4.html` (content only, design discarded) → mirrored verbatim in `src/data/content.js` (new-section copy like `MINI_CTAS` is additive). The "Signal" aurora specs under `docs/superpowers/specs/` are SUPERSEDED.

## Entrances — ONE simple fade everywhere except the home typewriter (2026-07-20, user rule)
The earlier per-page hero effects (LineWipe clip wipe, MaskRise, BlurRise, RailRise, and the per-word WordRise) were all judged unprofessional and are DELETED. The only survivors: Home = the typewriter + caret (`components/Hero.jsx`, untouched), and everything else — every other page's hero headline AND the big section headings (Problem statement, FinalCta lines, Testimonial first-in quote, AiAccelerated heading, Services discipline titles) — uses `motion/ink/FadeIn.jsx`: an opacity-only fade (650ms, EASE_OUT, `text`/`as`/`className`/`delay` contract, plus `children` for block use), fully static under reduced motion because `useInViewOnce` no-ops. Scroll reveals for body content stay on the gentle `components/Reveal.jsx` (fade + 16px rise, 700ms). Do NOT reintroduce per-word/per-char choreography, clip wipes, mask rises, or blur entrances. The ServiceDetail hero keeps its teal rail under the headline as a STATIC device that fades with the line (`.heroRail` in `ServiceDetail.module.css`) — it no longer draws itself.

## Hero headline type — one shared size, max 3 lines (2026-07-20, user rule)
`--fs-page-hero: clamp(2.4rem, 4.6vw, 4rem)` in `tokens.css` is THE hero-H1 size for every interior page (Services hub, the 4 detail pages, About, Contact, both case studies). It is sized so the longest headline holds ≤3 lines in a split-hero column — never let a page hero exceed 3 lines; fix with type size/wrapping, not copy. The Services hub headline flows its two copy segments INLINE (accent tail, nbsp orphan guard) instead of forcing a 2-line split that wrapped to 4. Only the home poster keeps its own oversized scale.

## MiniCta — interstitial CTA strips
`src/components/MiniCta.jsx` — slim ledger-row CTA band between sections: **`--accent-soft` teal wash with an accent top-border** (strengthened 2026-07-16 from the old near-invisible `--accent-tint` so the strip reads as a deliberate teal band, not a grey gap — it's a beat in the homepage band rhythm), teal node + mono kicker, one Satoshi statement, primary `Button`. Copy in `content.js` `MINI_CTAS`. Placements (2026-07-16 content-spec sync): Home after "What We Engineer" (`homeServices`, §05b) and after Portfolio (`homeWork`, §08b), Services between disciplines 02 and 03 (`services`). Deliberately quiet so FinalCta stays the crescendo — don't add more per page without asking.

**Homepage band rhythm (into FinalCta):** `Portfolio` canvas → `MiniCta` teal strip → `Testimonial` `--bg-2` grey → `AiAccelerated` navy pre-crescendo wash → `FinalCta` deep navy. The wash uses `--prelude` (new token, both ink modes in `theme-ink.css`): a navy step between `--bg-2` and `--band` so the pre-finale section warms toward the dark crescendo instead of matching the grey Testimonial. FinalCta stays the single dark band.

## About — "The team sheet" (Deep Ink)
`src/pages/About.jsx` + `About.module.css`, custom hero. Hero: meta bar (`ABOUT` / "Small · Senior · Direct"), FadeIn split headline (line 2 accent), and **`components/HeroBuildBoard.jsx` on the right** (2026-07-20 — replaced the documentary `about-hero.webp` photo, which the user called off-style; every page hero now carries a coded moving vignette). The build board is HeroThread's sibling: ink console card (hairline frame, accent wash, diffusion shadow, slow `bbFloat`), header strip with pinging live dot + `tekniik / build-board` + "This week" chip, a **6-row cycling sprint ledger** (2026-07-20 round 2, user: "show a lot in the list… each item loading and shipped… and repeat" — was 4 static rows): **task-type glyph chips** (IconPen design / IconCode API / IconCard payments / IconGrid admin / IconFlask QA / IconDeploy launch — IconCard + IconGrid ADDED to `Icon.jsx` 2026-07-21; the chips originally held fake team initials "SM/AR/…" which the user flagged as meaningless — don't bring initials back), Satoshi task names, mono verdicts. React `phase` state (-1 all-Queued → 0..5 that row Building → 6 all-Deployed hold → reset, repeat; STEP 2600ms / HOLD 3400ms — slowed from 1500/2600 2026-07-21, user: "moves too fast"; verdict copy is "Building" (not "In build"), rows resolve to "Shipped" EXCEPT the last row (Launch review) which reads "Deployed" — all same-day user feedback — driven by a setTimeout chain started in the `useInViewOnce` play() so it never runs under RM) derives each row's status; the Building row's 2px progress bar draws 0→1 via `bbLoad 1.9s linear forwards` — **deliberately ~700ms shorter than STEP** so the full bar settles before the row flips and the next line starts (user: "going to the next line is too quick"; restarts because the verdict remounts per phase), verdict swaps fade-rise in via `bbStateIn`. `shown = reduced ? ROWS.length : phase` — RM renders the finished sprint statically. **No aspect-ratio on `.wrap`** — six rows size the card; a fixed 4/3 box clipped rows + foot at ≤414 and ~1024. And a "One team → Always shipping" footer link carrying the **five-stage delivery pipeline** — `IconBulb → IconPen → IconCode → IconFlask → IconDeploy` (ideation/design/dev/test/ship; Bulb/Pen/Flask/Deploy were ADDED to `Icon.jsx` for this — chosen to stay crisp in a small chip. Ship-glyph history: `IconRocket` was mush at size, a paper plane read as "send a message", an up-arrow-from-tray wasn't deploy-y enough — per user, IconDeploy is now the canonical **cloud-upload** glyph, lucide-style cloud hull + rising arrow) as 24px circular chips (the ledger glyph-chip idiom: `--bg-raise` + hairline border, punched out of the line, `flex-shrink: 0`). **The run effect (user: "process start to end and repeat"):** `.linkFill`, a 2px teal line, draws left→right over the hairline (`scaleX`, linear so its edge lines up with the space-between chip positions 0/25/50/75/100% ↔ keyframes 2/17/32/47/62%); each chip ignites (accent color/border + bloom shadow + scale pop, ship pops biggest) as the line reaches it and **stays lit**; the completed run holds to 80%, everything fades together, rests, repeats on a shared 7.2s clock. Stage timing is per-chip explicit keyframes `bbStage1..5` mapped by `:nth-child` (a shared keyframe + `animation-delay` can't do stay-lit-then-reset-together — the delay would shift the reset too). Evolution, all user-driven 2026-07-20: ping-pong dot → one-way arrow ("dot read as meaningless") → icon wave ("show the actual process") → this drawn-line run ("icons not prominent, deploy icon looks bad, process start to end"); don't regress to abstract travellers or brief non-holding pops. ≤480px chips 18px; ≤380px pipeline hides (plain hairline stays — foot tags leave no room). One-shot timeline on scroll-in: rows land staggered, then the API row flips IN BUILD → SHIPPED. The CSS default state IS the finished board, so reduced motion (init never runs) renders it complete; under RM the pipeline shows the finished run — line fully drawn, every chip lit. A 4-cell signals ledger closes the hero. Then (order follows the about-spec table — `01 STORY → 02 PRINCIPLES → 03 PROCESS → CTA`, reordered 2026-07-17 from the earlier Story→Process→Principles): `01 / OUR STORY` (heading left, hairline-railed paragraph column right — first paragraph is the lede, last is a teal-railed Satoshi pull statement. `.storyCta` — a primary arrow Button "See our work" → `/work` beside a mono "50+ projects delivered" proof note — sits in the LEFT column directly under the "Why Tekniik exists." heading, inside the `storyHead` Reveal (moved there from the end of the paragraph column 2026-07-20, user: "below the left side heading")), `02 / PRINCIPLES` (2×2 cells, 2px `--border-strong` top border → accent + lift on hover), `03 / PROCESS` (brand-navy band with tokens re-scoped like the Services AI section; **five** steps — Listen / Plan / Build / Launch / Support & Grow, matching the homepage PROCESS and the spec's 5 phases — each top hairline fills teal in sequence via `useScrollProgressInk` p·5−i with igniting nodes, ghost numerals, mono "YOU GET" win lines), FinalCta. Process is now the last section before FinalCta (spec puts the dark timeline directly before the dark CTA), so it uses **`--band-deep`** (`#141d40`) instead of `--band`: the deeper navy steps UP into FinalCta's brighter `--band` + teal bloom, so the two adjacent dark bands read as a crescendo rather than merging.

## Contact — "The open line" (Deep Ink)
`src/pages/Contact.jsx` + `Contact.module.css`, custom hero (PageHeader dropped; `contact-hero.webp` unused — see ISSUES.md). Hero: meta bar (`CONTACT` / "UK · Chennai / one working day"), then a split — FadeIn headline + sub left, **`components/HeroThread.jsx` right** (the "what working with us looks like" message card, 2026-07-20 round 2 — user: keep every hero vignette looping with a ≥3-beat narrative, "show other message… addressed the issue… resolved". The thread now LOOPS through two exchanges stacked in one grid cell (`.phases`, both `grid-area: 1/1`, so the card height never jumps): **A** = enquiry bubble → typing indicator → navy reply → "First reply · 21 minutes" stamp; A fades up-out, then **B** = launch-week bug report → typing → same-day-fix reply → "Issue resolved · same day" stamp; holds, whole body fades, repeats (`createTimeline({ loop: true })`). **Loop-reset gotcha:** the t=0 resets are real explicit-from tweens (`opacity: [0,1]`, 1–200ms), NOT `.set()` — zero-duration sets only fire on the first pass, so a `.set()`-based reset leaves phase A invisible on every later loop. The typing bubbles are absolute over each reply's reserved space and `opacity: 0` in CSS by default; `.phaseA` is `opacity: 0` in CSS so the static/reduced-motion render is the RESOLVED second exchange with no overlap. Perpetual life = dot ping + slow `htFloat` card drift; decorative `aria-hidden`, hidden ≤960px; user rejected the previous OpenLine arc chart as irrelevant) — then `.heroCta` — a primary arrow Button "Send us a message" (added 2026-07-20) that jumps to the form spread (`id="contact-form"`, `scroll-margin-top: 84px`): **must scroll via `getLenis()`** (`lenis.scrollTo(el, { offset: -84 })`, mirroring Hero/Services — native `scrollIntoView` gets cancelled by Lenis's rAF loop and strands the page at a random offset; RM falls back to instant `scrollIntoView`) — then a 3-cell **status ledger**: each cell is a mono-caps `.statusLabel` over a Satoshi-700 `.statusValue` (Availability / "Open for new projects" with the pulsing `--success` live dot · First response / "Within one working day" · Discovery call / "30 minutes, no obligation"), cells split by hairlines, first cell flush with the headline's left edge, each in its own staggered Reveal; stacks to one column ≤640px (2026-07-21 — user: the old flat mono-caps strip ("Available now / Replies within 24 hours / Discovery call · 30 min") "looks amateur"; don't regress to single-line caps cells). Working spread: **ledger form** left (mono caps labels, Satoshi input text, transparent inputs over hairlines; focus = a teal 2px underline draws in via scaleX — the reset's `--focus-ring` box-shadow is suppressed on `.input` because the underline is the focus indicator; the textarea is `resize: none` per user) — **live EmailJS form, 2026-07-21** (see the EmailJS section below): client-side validation + a `'idle' | 'sending' | 'sent' | 'error'` status, and on success the whole form is REPLACED by the `SentPanel` card; right = "Prefer to talk directly?" **direct-lines ledger** (Email / Careers / WhatsApp rows: mono label + Satoshi value, accent-tint sweep + arrow on hover — the sweep `::before` sits FLUSH with the list edges (`inset: 0`, `--r-lg` corners) and on hover the label+value `translateX(12px)` while the arrow settles at `translateX(-12px)`, faking 12px inline padding with transforms (2026-07-21 second pass, user: the earlier −12px bleed past the list "does not look good" — don't re-bleed it; real padding is out because subgrid eats inline padding and padding animates layout); `.lines` is a grid with `subgrid` rows so all values share one left edge — ≤560px the rows stack label-over-value instead) + availability line + teal-railed **office card with flag tabs** (Chennai/London, same `IconFlagIndia`/`IconFlagUK` + tablist pattern as the Footer; addresses come from the shared `OFFICES` const in `content.js`, which the Footer also consumes — edit office data there only). Closing dark beat: **"What happens next?"** on the brand-navy `--band` with tokens re-scoped — REDESIGNED 2026-07-21 (client: the old floating 2px rail dashes "look bad"): a **compact 4-column status-ledger band** — each cell = 46px circular **icon chip** (IconMail read / IconReply / IconPhone call / IconCheck no-strings; IconReply ADDED to Icon.jsx; IconHandshake was tried for step 4 and is mush at 20px — don't bring it back) over a mono timing stamp over a Satoshi-700 statement, cells split by vertical hairlines (first cell flush-left, the hero status-strip idiom). Scroll choreography via `useScrollProgressInk` p·4−i: each chip's `.chipFill` **fills teal bottom-up (scaleY tracks local progress — a liquid rise, user-requested)**, and past 0.55 the cell ignites (`.stepLit`: glyph flips to `var(--bg)` band navy, chip border→transparent + bloom, stamp→accent, statement dim→bright; 420ms transitions). `CONTACT_PAGE.side.next` is now `{text, stamp}` pairs (timing pulled out of the sentences into the stamps — no facts changed). Responsive: 4-col → 2×2 ≤960 (hairlines re-derived: odd cells lose border-left, cells 3-4 gain border-top) → 1-col stack ≤640 (`.nextStep + .nextStep` needs `border-left: none` explicitly — the base sibling selector outspecifies a bare `.nextStep` reset). Design history, both same-day user-rejected: the original four columns with floating scroll-lit 2px top dashes ("lines look bad"), then a full-width vertical "open line" thread ledger with ghost numerals ("not appealing, no need such a big section"); ghost `0N` numerals then replaced by the icon chips per user. RM: hook fires once p=1 → all chips full + lit, transitions off. **No FinalCta on this page on purpose** — the form is the CTA; `CONTACT_PAGE.finalCta` stays unused.

## Contact form — EmailJS wiring (2026-07-21)
`@emailjs/browser` sends the Contact form client-side; credentials live in `src/config/emailjs.js` (`serviceId` `service_0y1838e`, `templateId` `template_4w6dj49`, `publicKey` `_ScRb32MA7m0DOobf`). All three are public by design — EmailJS is a browser service and they ship in the bundle — so they are literals with `VITE_EMAILJS_*` env overrides (documented in `.env.example`) for staging/forks. There is **no backend**; don't add one for this form. The payload sends each field under BOTH conventions (`name`/`email`/`message` **and** `from_name`/`from_email`/`reply_to`) so either EmailJS template naming resolves — extra template vars are ignored by EmailJS, missing ones are not.

**Validation** (`RULES` + `validate()` in `Contact.jsx`): name ≥2 chars, email regex, message ≥10 chars. A field is validated on blur and on submit, and only **re-validates while typing once it has already failed** — a fresh field never scolds you mid-word. Submit blocks and focuses the first invalid field. Error UI is the message text alone in mono `--danger` plus the input's own hairline turning `--danger` — **do NOT draw a 2px rail under the error message** (user: the full-width line "looks bad"). The focus rail lives in a `.control` wrapper around the input, NOT on `.field` — with the error span inside `.field`, a `bottom: 0` rail lands under the *message* instead of the input.

**Sent state** — `SentPanel` replaces the form entirely (not a button swap): a **centred card** (`--bg-raise`, hairline, `--r-2xl`, `--shadow-card-hover`, all text centre-aligned — user spec) whose teal top rail draws across on `railDraw`, with an SVG check mark that draws ring-then-tick via `stroke-dashoffset`, a teal `--accent-tint` bloom behind, and stamp/heading/copy/"Send another message" lifting in on staggered `sentIn`. In-flight the CTA shows a spinner and `Button` gets `disabled` (new `.btn:disabled` rule in `Button.module.css`). Failure renders a mono `--danger` line with the `mailto:` fallback. Reduced motion: every keyframe ends where the static state already is, and the RM block kills them all, so the card renders complete.

## Nav CTA — Get a Quote hover ring (2026-07-21, user rule)
The header `a.ctaBtn` hover ring in `Nav.module.css` is a **two-tone brand conic** (`#72ccd6` teal ↔ `#202e5d` navy, seam-safe: starts and ends on teal) spun via the `--gq-angle` `@property`. It was previously a 5-stop Gemini-style multicolor gradient — the user rejected anything beyond the two primary brand colors; don't add stops back.

## Nav typography
Nav items (`.link`, `.mobileLink`) are hard-set to `'Satoshi', var(--f-display)` — the header must read Satoshi on every route, including non-inked ones where `--f-display` is still Inter Tight (user request).

## Nav theme toggle — animated sun/moon swap (2026-07-20, user: "looks dead")
The `.themeToggle` button keeps **both icons mounted** inside `.toggleIcons` (17px relative box, icons absolutely stacked, `aria-hidden`); `styles.toggleDark` on the button (from `themeMode === 'dark'`) drives a CSS crossfade-rotate: the outgoing icon rotates away (`rotate(±120deg) scale(0.5)` + fade) while the incoming one rotates in, transform 360ms `--ease-out` / opacity 200ms. Transitions, not keyframes, so rapid toggling interrupts smoothly. Don't go back to conditional `{themeMode === 'dark' ? <IconSun/> : <IconMoon/>}` rendering — an unmount/mount swap can't animate. RM: transitions off.

## Nav links — curved dash + text roll (2026-07-20, iterated ×6 with user, FINAL per user spec)
The desktop nav links are **bare Satoshi 700 text at 0.9375rem**. **Active** = `.marker` (per-item, `aria-hidden`): a 3px accent **dash under the label with BOTH top corners curved** (`border-radius: 999px 999px 0 0`, flat bottom — the user's exact spec, "normal dash but top-left and top-right border-radius curved"), spanning the label (`left/right: 12px` = the link padding), entering with fade + `scaleX(0.5→1)` from center, 260ms `--ease-out`. **Hover** = a **text roll**: each NavLink renders `.roll > .rollTop + .rollBottom` (duplicate label, `aria-hidden`, ink-colored) inside `overflow: hidden`; hover slides top out (`translateY(-110%)`) and bottom in, 280ms `--ease-out`, gated behind `(hover: hover) and (pointer: fine)`. Press = `scale(0.97)`. Everything is **pure per-item CSS — no JS measurement, no sliding shared element** (the previous `useEffect`/refs/`useLocation` positioning code was deleted from Nav.jsx). Design history — all earlier treatments user-rejected, do not reintroduce: the boxed frosted pill / segmented control ("looks like a tab"), a plain square-ended sliding underline ("not just a dash"), mono `01`–`04` index numerals ("no need numbering"), the sliding viewfinder corner brackets ("don't like the effect"), the glowing node dot / signal lamp ("dot is not looking good"), and a top-right-only-rounded dash with a chevron tip ("looks bad"). Marker + roll colors come from `--accent`/`--ink`, so both ink modes adapt with zero overrides. Reduced motion: all roll/marker/link transitions off (active marker renders complete), hover falls back to a plain in-place recolor (`.link:hover { color: var(--ink) }` inside the RM block).

## Stack
- Vite 8 + React 19 (with `react-compiler`)
- `react-router-dom` (BrowserRouter, in-app SPA navigation)
- Vanilla CSS Modules per component — no Tailwind, no UI kit
- **Motion layer** (`src/motion/`): **anime.js v4** (no framer-motion) + `lenis` smooth scroll (`SmoothScroll.jsx`, `getLenis()`). Ink barrel `src/motion/ink/index.js`: `useInViewOnce` (init/play/cleanup, no-ops under reduced motion), `useMagneticInk`, `useScrollProgressInk`, easings `EASE_OUT`/`EASE_INOUT`. The ONLY entrance component is `motion/ink/FadeIn.jsx` (see the Entrances section) — WordRise/LineWipe/MaskRise/BlurRise/RailRise were deleted 2026-07-20. `components/Reveal.jsx` (+ `hooks/useReveal.js`) is the IO-driven scroll reveal for body content.
- All SVG icons inline (`src/components/Icon.jsx`). Rating stars: the `★` characters in `content.js` copy (Hero trust, proof ticker, Numbers band) are NOT rendered as text — `StarredText` (Icon.jsx) splits the string and swaps each `★` for `IconStar`, a filled round-jointed (blunt-point) star sized in em. `IconStar` sets `display:inline-block` inline because `reset.css` blockifies all `svg`.
- **Satoshi is self-hosted** — `public/fonts/satoshi/*.otf` with **every shipped file registered** in `tokens.css` (300/400/500/700/900, normal + italic). It was previously loaded from `api.fontshare.com`, which began returning **HTTP 200 with a body of `/* Access to the Fontshare API has been temporarily restricted */` instead of CSS** — so no console error, no failed request, Satoshi just silently ceased to exist and every `--f-display` use fell through to the `'Inter Tight'` fallback. Do NOT reintroduce the CDN `<link>`. `index.html` preloads Black + Medium so the hero typewriter doesn't swap mid-type. **Satoshi call sites use ONLY real file weights — never 600/800** (user rule, 2026-07-20: no synthesized weights). The old display-800s were snapped to 900 (Black) and the Satoshi-600s to 700; `font-weight: 600` remains fine on `--f-mono`/`--f-body` because the Google Inter load includes a real 600.
- Google Fonts: **Inter Tight** (display/headlines, heavy 800–900) + **Inter** (body) — `--f-display` / `--f-body` tokens in `tokens.css`. `--f-mono` is ALSO Inter now (the old IBM Plex Mono read as off-brand "code" per user); the token is kept as a distinct slot so the tiny uppercase-tracked labels/index numbers can be tuned independently, but it points at `'Inter'`.

## Homepage "Bold Editorial" theme (light)
The homepage `/` is a bold, left-locked editorial redesign; other routes use the base light theme. The look is driven by the design tokens + a scoped `data-theme="light"`:
- **Scoping:** `App.jsx` sets `data-theme="light"` on `<html>` via `useLayoutEffect` (pre-paint, no FOUC) when `location.pathname === '/'`, and removes it elsewhere. `theme-light.css` (`:root[data-theme='light']`) overrides the base semantic tokens for the homepage. `theme-dark.css` still exists for a possible future dark toggle but is NOT part of this design.
- **Palette (restrained indigo, high contrast):** clean white canvas (`--bg:#ffffff`), faint band `--bg-2:#f4f5f8`, solid white cards (`--surface`, no glass), near-black ink (`--ink:#0a0b10`), crisper hairlines (`rgba(10,11,16,.12)`). Single-hue indigo: `--accent:#5b5bff`; the old violet/magenta aurora stops (`--accent-2`/`--accent-3`) are aliased back to indigo and `--grad-aurora` is a mono indigo→deep-indigo gradient. There is **no page-wide dot/aurora wash** (removed from `global.css`).
- **Inverted band tokens (theme-agnostic, in `tokens.css`):** `--band:#0a0b10`, `--band-2`, `--band-raise`, `--on-band`/`--on-band-dim`/`--on-band-faint`, `--band-hairline`, `--accent-on-band:#8b8bff` (lighter indigo for legibility on near-black). Used by the featured Capabilities card, Portfolio featured/hover-invert rows, and FinalCta (Testimonial is now light).
- **Sharp radii:** `--r-sm:3 / --r-md:4 / --r-lg:6 / --r-xl:8 / --r-2xl:12`. Buttons use `--r-lg`.
- **Editorial devices (recurring):** each major section header is `NN | EYEBROW` (mono index in `--accent` + mono uppercase eyebrow behind a hairline) above an oversized Inter Tight heading. Section index numbers run 02–07 (Hero has the meta bar instead). The Hero is an oversized poster + full-bleed proof-ticker marquee (no cascade); inverted bands (featured Capabilities card, Portfolio, FinalCta) as the contrast rhythm.
- **Nav:** the base light Nav (floating pill) reads over the white hero; it also sits over the inverted Testimonial/FinalCta bands as a light glass bar when scrolled — fine.
- Never hardcode hex outside `tokens.css` / `theme-light.css` (small rgba overlays excepted).

## Layout
```
src/
  main.jsx                     entry; mounts BrowserRouter
  App.jsx                      routes, Cursor, Nav, Footer, ScrollToTop, route fade
  styles/
    tokens.css                 design tokens (color, type, spacing, motion)
    reset.css                  reset + base type
    global.css                 body, route fade, skip link (dot wash removed)
  hooks/
    useReveal.js               IntersectionObserver fade+rise
    useCounter.js              RAF count-up with easing
    useReducedMotion.js        prefers-reduced-motion media query
    useMagnetic.js             cursor magnetism (pull primary CTAs)
    useScrolled.js             scroll-position threshold
  data/
    content.js                 verbatim copy from prototype-v4.html
  components/                  primitive + shared components
  sections/                    page-level sections (used on Home)
  pages/                       Home, Services, About, Contact, CaseLooqz, CaseAutoScreen, NotFound
```

## Conventions
- One CSS module per component file. Class names locally scoped.
- Tokens come from `styles/tokens.css`. Don't hardcode hex outside tokens (exceptions: traffic-light dots, code syntax colors).
- Single accent (`--accent: #5B5BFF`). No second accent. No gold. No warm cream.
- `prefers-reduced-motion` honored everywhere: tokens collapse animation durations to `0ms`, `Reveal`/`KineticText` fall through (MotionConfig `reducedMotion="user"`), scroll-linked values gate via `reduced ? 1 : value`, per-module `@media (prefers-reduced-motion: reduce)` kills hover transitions.
- All animations are GPU-friendly (`transform` / `opacity` only). Never animate `width/height/top/left/padding/margin`.
- Touch targets ≥ 44×44px. `min-height: 44px` on buttons, hamburger 44×44, mobile nav links min-height 56px.
- Focus rings: custom indigo ring via `--focus-ring`. Never `outline: none` without a visible alternative.
- Semantic HTML: `<header>` for nav, `<main>` for main content, `<section>` per logical section, `<article>` for portfolio cards, `<footer>`.
- Skip-link at top of `App` for keyboard users.

> `Home.jsx` order (2026-07-16 content-spec sync — reordered to the homepage spec's section table): `Hero → LogoStrip (Stats, 03) → Problem (Empathy, 04) → ServiceShowcase (What We Engineer, 05) → MiniCta (homeServices) → Why (06) → Process (07) → Portfolio (08, id="work") → MiniCta (homeWork) → Testimonial → AiAccelerated (10) → FinalCta`. Section index numbers now run 03–08 then 10. Each section renders verbatim `content.js` copy. Non-home routes unaffected.
>
> **Content-spec sync (2026-07-16):** the live site was aligned to the finalized `/content/*.md` locked specs. Homepage copy shifted to "software / engineer" positioning: HERO eyebrow "Custom Software · Web Platforms · Mobile Apps · AI Systems", "engineer" subtitle, ghost CTA "See our work" (smooth-scrolls to `#work` via `getLenis()` in `Hero.jsx`, mirrors the Services index), trust "4.9★ · 50+ Projects · 98% Retention". Proof-ticker labels (`FRAME_LABEL` in Hero.jsx) → Custom Software / Web Platforms / Mobile Apps / AI Systems. `CAPABILITIES` renamed to "WHAT WE ENGINEER", reordered Custom Software(key `app`) / Web Platforms(`web`) / Mobile(`mobile`) / AI(`ai`) — keys still map to their ServiceVignettes scene, rows link via each item's `to` (→ /services until Phase-2 sub-pages ship). `PROCESS` split 4→5 phases (Listen/Plan/Build/Launch/Support & Grow); last phase's rail gets an open-ended teal chevron cap. Portfolio countries → real (UK / South Africa), and the two routed case studies were geo-rewritten to match (GlowBook France→UK, ScreenFix Germany→South Africa incl. €→R pricing); affected `TESTIMONIALS` attributions realigned. Footer legal links wired to `/privacy-policy` `/terms-of-service` `/cookie-policy` `/gdpr` (pages land in a later phase).

## Hero — "Poster + proof ticker"
`src/components/Hero.jsx` is a full-width editorial **poster** on the clean white canvas (no AuroraShader, no dot grid, no side cascade — the old split read as "too normal"). Top **meta bar**: `HERO.eyebrow` left + `HERO.trust` right across a hairline (stacks ≤480px). Below it, an **oversized flush-left headline** built from `HERO.headline` words — `Technology` / `built ` + `right.` (final word `--accent`) at `clamp(3.4rem,12vw,10rem)`, line-height 0.86. A **hand-drawn indigo marker** (`<Marker/>` inline SVG) draws in under "right." via a CSS `stroke-dashoffset` keyframe (instant under reduced-motion). A `.lower` flex row holds the sub copy (left) + CTA cluster (magnetic filled `Button` via `useMagnetic` + ghost, right; stacks ≤760px). At the very bottom, a **full-bleed infinite proof ticker** (`.ticker`, outside `.container`, `aria-hidden`): `TERMINAL_FRAMES` rendered twice back-to-back as a `.tickerTrack` marquee (`translateX 0→-50%`, `34s linear`, pauses on hover, edge-faded via `mask-image`), each item = mono label (`Websites`…) + big Inter Tight `result` value + indigo dot separator. Reduced-motion → marquee + marker both static. `ServiceShowcase` is its OWN section rendered by `Home.jsx`.

**Ghost-word gradient fill:** the middle word `built` is `.ghostWord` — **no stroke** (user removed it), filled with a **drifting brand gradient** clipped to the glyphs via `background-clip: text`: 90deg periodic `--accent-deep → --accent → color-mix(60% --accent, white) → --accent → --accent-deep` at `background-size: 200%`; the light-teal highlight band exists because accent↔accent-deep alone are too close in value for the motion to read. `ghostFlow` animates `background-position` by exactly one tile per 4s → seamless endless drift. Reveal gate: the clipped background paints from glyph geometry regardless of the per-char `opacity: 0` (it showed "built" as a gradient while other words were still typing — user bug), so **no `background-image` exists on the base `.ghostWord`** — the gradient lives in a `--ghost-grad` custom property, chars type in as solid `--accent`, and only `styles.headlineDone` (added by `finish()`) attaches `background-image: var(--ghost-grad)` + the `ghostFlow` animation while `color` transitions to transparent (700ms crossfade). Known exception to the transform/opacity rule (background-position repaints); scoped to one short word. **Caret:** `.caretBlink` runs `caretBlink` 5 iterations + `forwards` (keyframes end at opacity 0), so after typing the caret blinks 5× then stays gone (user request — was infinite); the pre-type pause reuses the class but removes it after ~650ms. Also: `HeroCircuit.jsx` traces deliberately stay clear of the proof-ticker band — the old horizontal `hc-p2` trace (y=720) ran through the marquee and read as a glitch (user), so it was removed entirely, and `hc-p1` terminates in a pad node at y=636 instead of running off-canvas through the ticker. **Circuit electrons wait for the entrance (2026-07-21, user rule):** `.electron` is `opacity: 0` until `styles.circuitLive` lands on the hero section root; `finish()` schedules that class via `setTimeout(tl.duration + 120)` — a timer keyed off the entrance timeline's computed duration, NOT anime's `onComplete`/`.then()` (the v4 timeline callback did not fire here; don't switch back without verifying live). Electrons then fade in over 600ms mid-flow (SMIL keeps ticking while hidden, negative `begin` offsets). Static traces/pads stay visible from first paint; under RM `play()` never runs so the class never lands, and `.electron` is `display:none` there anyway. `.tickerValue svg` gets `display:inline` + em sizing in `Hero.module.css` because reset.css makes all svg `display:block`, which snapped `StarredText`'s inline star onto its own line. Reduced-motion: media query sets `.ghostWord` color transparent + `background-image: var(--ghost-grad)` + `animation: none` (static filled gradient) since `useInViewOnce` no-ops and never adds the class.

## ServiceShowcase — "What We Engineer / Capability index" (`05`) + animated vignettes
`src/sections/ServiceShowcase.jsx` renders `CAPABILITIES` as a **full-bleed, one-service-at-a-time immersive stage** (rewritten 2026-07-16). The section header is **one line** (`What we engineer.` — inline `.headingLine`/`.headingAccent` spans, accent on the 2nd word) with the **description on the next line**, both spanning the width (no dead right-whitespace). **(2026-07-17 layout change, user request)** each pinned `.panel` is now a **split composition — copy column left, tall device screen right**: the `.overlay` (title, copy, teal "Explore …" link) is a `clamp(340px,34vw,540px)` left column (vertically centred, `padding-left: var(--edge)`, `overflow:hidden` to clip the ghost), and the `.stage` (edge-faded blueprint-grid `::before`, drifting accent-bloom `::after`) is the flex-1 right column bleeding to the viewport's right edge. Inside it the **`.screen`** — a monitor-like frame (border, `--r-xl`, `--surface`, shadow) with a **`.screenBar` window-chrome strip** (three dots, first accent) above the `.sceneHolder` — stands nearly full deck height (`calc(100% - clamp(10px,2vh,28px))`), left edge at the copy column, right edge on the container line (`right: var(--edge)`), so the vignette reads as a computer screen inline with the Explore link. Non-pinned keeps the stacked copy→stage flow but gains the same `.screen` chrome (stage gets padding so the grid shows around the frame; sceneHolder stays `16/10`, `4/3` ≤640px). The `.desc` height reservation is now 6 lines (narrow column wraps more). The **giant faint ghost word** = the **full service name** uppercased (e.g. `CUSTOM SOFTWARE`) is a **panel-level watermark spanning the whole composition** (direct child of `.panel`, z 0; the pinned `.stage` gets `z-index:1` so the word passes BEHIND the screen frame and shows through its translucent `--surface` bg — 2026-07-17 user request "bigger, left to right behind the vignette"), sized `clamp(5rem,11vw,13rem)`, **drifting left↔right ±5% of its own width** on the `ghostDrift` loop (the `.ghost` wrapper keeps the scroll parallax, `.ghostInner` animates); the pinned `.pin` uses `overflow: clip` (NOT `hidden` — hidden would break `position: sticky`) so drift extremes can't cause h-scroll. The headline **rises up "out of" the copy block** word-by-word — each word sits in an `overflow:hidden` `.titleMask` and translates `116% → 0` when the panel becomes active (staggered via inline `transition-delay`). Scenes live in `src/sections/ServiceVignettes.jsx` + `ServiceVignettes.module.css` — one per capability key: `web` (self-assembling marketing site, cursor clicks the CTA on a 7s loop; **the browser now sits on a laptop** — `.laptop` shell: `.lapScreen` navy bezel (webcam dot in the top bezel) + `.lapBase` slim deck with a trackpad-notch scoop; the entry rise moved from `.browser` to `.laptop`, and `.cursor`'s base position is the click point which MUST track the CTA — currently `19% / 58%`; re-measure the CTA centre as % of `.sceneHolder` if the layout shifts), `app` (live dashboard: breathing bars, self-drawing SVG trend line, wandering sidebar signal; **the dashboard now runs on a desktop monitor** — `.monitor` shell: `.monScreen` navy bezel with a teal `monLed` power dot in the bottom bezel + `.monNeck`/`.monFoot` stand; entry rise moved from `.appWin` to `.monitor`), `mobile` (**two devices, 2026-07-17 user request** — a landscape 4:3 iPad behind-left + a slim 9:19.5 iPhone front-right, both width-only sized via `min(%, cqh·ratio)` so aspect NEVER distorts (the old `width + max-height` combo squashed the phone into a tablet-ish slab); both viewports run the SAME `screensLoop` keyframes so phone `MScreen` and tablet-landscape `TScreen` layouts scroll in sync = the responsiveness story; phone keeps island/tab-bar/notification-drop, 12s loop. **Realistic hardware shells (2026-07-21, user: tablet "looks like just a rectangular box")**: both devices wear a brand-navy bezel (linear-gradient of `--band-raise/--band/--band-2`, `--band-hairline-strong` edge — band tokens are identical in both ink modes) around an inset `--bg-raise` screen with a glass sheen `::after`; the tablet gets a `tCam` camera dot centred in the top bezel + power/volume buttons via the shell's own pseudos, the phone gets `.pBtn` side buttons, an island with a lens dot, a `pStatus` strip (time dash + 3 signal bars), and a `homeBar` gesture pill under the tab bar — phone innards now live inside a `.pScreen` wrapper, so island/status percentages resolve against the screen, not the shell), `ai` (SVG pipeline: doc node → conic-ring "thinking" core (orb centre) → SMIL packets flowing to three outcome chips; **plus a microchip node below the core (2026-07-21 round 2)** — the chip was FIRST placed inside the spinning ring, which the user rejected ("looks bad — put that AI chip somewhere and show connection"), so the orb is restored and `.aiCpu` now sits at `54.5% / 82%`: a navy rounded die (`--band-raise→--band-2`, `--accent-border` edge, `--glow`) with a mono `AI` label in `--accent-on-band` and teal pin legs on all four edges via masked repeating-gradient pseudos, wired into the pipeline by `AI_PATHS.cpu` (dashed wire + upward SMIL packet feeding the core = the AI engine powering it); don't move the chip back into the ring). Each scene floats its capability's `TERMINAL_FRAMES.result` as a metric chip and animates only when its panel is active (keyframe loops gate on the `active` class so idle scenes cost nothing). Rules the vignettes obey: **all color from theme tokens** (both ink modes adapt automatically), pure CSS motion, transform/opacity only, reduced-motion blanket-kill at the bottom of the module (active scene renders complete + static; SMIL packets hidden via `display:none`). `.sceneHolder` is the **size container** — scene metrics use `cqw`/`cqh` so compositions scale proportionally at every viewport. **Pinned immersive scroll-through (desktop):** on `min-width:961px` + `pointer:fine` + motion-ok the section sets `data-pinned="true"` and the deck **pins** — a `440vh` wrapper (`.pin`) with a `100vh` sticky inner (`.pinInner`); the deck goes **full-bleed** (`width:100%`, no container max/gutter). **Fit is height-driven so the whole composition always sits inside the viewport below the nav — this is what killed the "jittery" feel (the earlier fixed-height composition overflowed short viewports and clipped top/bottom as you scrolled):** the sticky `.pinInner` is top-padded to clear the fixed nav, the pinned `.deck` is `flex:1` (min-height:0, max-height:860px) so it fills only the space the padded inner leaves, and each `.panel` (absolute `inset:0`) is a flex column where the `.overlay` copy is `flex:0 0 auto` and the `.stage` band is `flex:1 1 auto` (min ~30vh, max 560px) — the band grows to fill whatever height is left under the copy, so nothing overflows at any viewport height. The `.overlay` is container-aligned (`max-width:var(--max)` + gutter) and the band's `.sceneHolder` "screen" is centred to the container width (`left:50%`+`translate(-50%,-50%)`, `width:min(100% - 2·gutter, var(--max))`) via `--edge` (`calc(max(0px,(100% - var(--max))/2) + var(--gutter))`). **`.desc` reserves a fixed `min-height` (tallest of the four copies)** so the copy block — and therefore the flex band + framed screen — stays the SAME size across all services (no size-jump on cross-fade). Parallax is deliberately **small** (`--p` × a few px on overlay/scene/ghost) so boundary snaps are imperceptible. Panels overlay + cross-fade (only `.panelOn` is `opacity:1/visible`, the rest `opacity:0/visibility:hidden`; `.stage` is `overflow:hidden`). A rAF-throttled scroll handler (rides the Lenis `scroll` event, `getLenis()` from `SmoothScroll.jsx`) maps scroll progress across the wrapper to the active service `floor(p·4)` **and** writes the per-service local progress `--p` (0→1) onto the deck; the active panel's `.sceneHolder`, `.ghost`, and `.overlay` read `--p` (a few px each) with **no transition**, so the subtle depth tracks the wheel frame-for-frame — an immersive scroll, not a slider. **Service index pane (2026-07-21, client request — REVERSES the earlier "no nav UI" rule):** the client flagged that a non-scroller would think there's only ONE service, so the pinned deck now carries `.svcIndex` — a compact ledger nav (mono `SERVICES · 04` kicker + four `01–04` + Satoshi-700 name rows) absolutely anchored bottom-left at `var(--edge)` (z 3, above the overlay), always on screen while the deck pins. State idiom matches the site's scroll-drawn teal rails: each row's bottom hairline is the progress rail — done rows hold a dim (`opacity .35`) full teal fill, the active row's fill tracks the scroll frame-for-frame via `scaleX(var(--p))` (inherited from `.deck`, NO transition on the LIVE rail — non-active rails get a 240ms transition for the hover preview; the `:not(.rowOn)` split is load-bearing, a transition on the live rail would lag the wheel), future rows sit empty; active name inks to `--text`, num to `--accent` (200ms color transitions, hover gated `(hover:hover) and (pointer:fine)`, `:active scale(0.98)` press). **Interaction layer (2026-07-21 second pass, user: "let users know something is coming up, interactive and intuitive"):** (1) `.svcDot` — a 5px teal node absolutely at `left:-14px` that scales in on the active row (220ms) and so "walks down" the index as the user scrolls; (2) `.nextTag` — a mono-caps accent "NEXT ↓" chip on the row AFTER the active one (`i === active + 1` → `.rowNext`), fading/translating in 240ms, its `DownGlyph` breathing +2px on a 2s `nextNudge` loop — the coming-up cue; last service has no next row, tag simply absent; (3) `.rowGo` — a `JumpGlyph` right-arrow (`margin-left:auto`) on every non-active, non-next row that slides in on row hover (name also `translateX(3px)`); (4) hover on a FUTURE row previews its rail (`scaleX(1)` at `opacity .14`). Both glyph spans are `aria-hidden`; all new transitions + `nextNudge` are in the RM kill block (belt-and-braces — the index never renders under RM anyway). Rows are real `<button>`s (`aria-current` on the active one) — click jumps via `getLenis().scrollTo` to the middle of that service's scroll band (`(i + 0.5)/n` of the pin travel; native smooth `scrollTo` fallback). `@media (max-height: 720px)` hides the kicker + compresses rows so the index can't ride up into the centred copy. Non-pinned (stacked) mode hides it (`display:none` default) — all four panels are naturally visible there; RM never pins so the index never shows under RM. The earlier user quotes ("looks like a slider… we don't need numbering like 1/4") applied to a `NN/04` counter/slider UI — the named index list is the sanctioned replacement; don't add dots/arrows on top of it. Native scroll only — no scroll-jacking; releases to normal flow after the fourth service. Progressive enhancement: no `data-pinned` (touch/narrow/reduced-motion) = the panels **stack** in normal flow (ghost hidden, title words un-masked, `.overlay` copy static above the vignette card) and scroll naturally; an `IntersectionObserver` lights whichever panel is most centred so its vignette animates as it enters view (`rootMargin: -18%`, replaces the old auto-cycle interval). `useStageParallax` is no longer used here (still used by `Services.jsx`). Gotcha discovered earlier: **percentage padding on absolutely-positioned scene nodes resolves against the stage, not the node** — use px/cq clamps. `/img/services/*.webp` are no longer used on the homepage (Services page still uses them).

## LogoStrip — "The Numbers" signal field (`03`, REVAMPED again 2026-07-17)
`src/components/LogoStrip.jsx` — the v1 "instrument ledger" (hairline rows + tick meters) was also rejected (client: too quiet; wanted interactive/stunning/animated vignette), so §03 is now a **signal field**: a full-bleed **`--band-deep` dark stage** (band tokens are identical in both ink modes) with layered ambience and a **cursor-tracked light**. Layers back→front: faint dot grid (edge-fade masked), `.pulse` — a slow **breathing teal-navy radial** (`breathe` 9s keyframes, the "animated vignette"), static `.vignette` edge darkening, `.dotsHot` — the same dot grid in teal revealed only inside a cursor radial mask, `.aura` — a soft teal glow that **chases the cursor with a 600ms trailing transform**. Content: node + `03 / THE NUMBERS` meta bar + statement head (`NUMBERS_HEAD`, on-band with accent tail), then the **board**: four oversized readouts (numeric-only `MARQUEE` filter, ★ via `StarredText`, one-line details from `NUMBERS_DETAILS` in `content.js`) in a **single 4-up row** (`repeat(4, 1fr)`, uniform `clamp(3rem,4.8vw,5.2rem)` values, **no per-stat index numbers** — user request; an earlier asymmetric staggered grid was rejected as too spacious). **Two identical stat layers**: the readable base + an `aria-hidden` **hot clone** (`.layerHot`, absolute inset-0) that is all signal-teal + glow and only shows through a `radial-gradient` **mask at the cursor** — whatever the light touches turns teal. Pointer plumbing is **direct DOM, never React state**: one rAF-throttled `pointermove` writes `--sx/--sy` (section-space, drives dotsHot mask + aura transform) and `--bx/--by` (board-space, drives the hot-layer mask) as CSS vars; `pointerenter/leave` toggles a `hasLight` class (gated by `matchMedia('(hover:hover) and (pointer:fine)')`). **Count-ups + hot clone can't drift**: a single `useIgnition` hook (useReveal → one rAF easing 0→1, setState only inside rAF per the lint rule) drives every displayed value; both layers render from the same computed strings. Entrance on `.on`: stats fade-rise staggered (`--row * 130ms`), digits **ink-fill from a `-webkit-text-stroke` outline to solid** (color/stroke-color transition), rules draw in `scaleX`. `@media (hover:none)` → dotsHot/aura/hot layer `display:none`, the breathing pulse carries mobile ambience; columns 4 → 2 (≤900px) → 1 (≤520px). Reduced motion → pulse animation off, all transitions none (instant final state; ignition returns 1). Colors: band tokens + on-band rgba shades of brand navy/teal (the established inverted-band idiom).

## Problem — "The verdict" ledger (`04`)
`src/sections/Problem.jsx`: `04 | THE PROBLEM` meta row, full-width oversized `FadeIn` statement, editorial paragraph column, then a **split comparison ledger** — `PROBLEM.beforeCard` (muted, hatched `.lineBad`, danger badge) vs `afterCard` (`--accent-tint` bg, solid `.lineGood`, success badge) divided by one hairline. Each half ends in three **audit rows** (`BEFORE_ROWS`/`AFTER_ROWS`, local to `Problem.jsx` — mono label + verdict + meaningful fill width, all derived from the beforeCard/afterCard copy: Timeline "4 months late"/34% vs "On time"/96%, Scope, Documentation; the earlier unlabeled tight bars read as a skeleton loader per user). **The bar choreography is the storytelling** (fires once via `useReveal` → `.play` on the ledger, which also owns the entrance fade — no `<Reveal>` wrapper): the agency side fills in a stuttering crawl (`barStall`, clip-path inset so the hatch doesn't stretch — deliberate exception to transform/opacity, composited + one-shot) that stalls twice and settles dim (opacity .72); the Tekniik side sweeps in fast (`barSweep` scaleX, `--ease-out`, 120ms stagger — glow scales with the fill) and a single light sheen (`barSheen` on `::after`) passes over after landing — done while the other side is still limping. Each verdict (`--danger` vs `--accent` mono caps) stamps in via `valueIn` as its own bar lands/dies. Per-row delays via inline `--i` on `.auditRow` (inherited by fill + value). Reduced motion → ledger, bars, and verdicts render complete/static, sheen hidden. Ledger stacks 1-col ≤820px.

## Why — bold four-up (`05`)
`src/sections/Why.jsx` — `06 | WHY TEKNIIK` head: oversized heading "We do things *differently.*" (accent tail — `WHY_TEKNIIK.heading` + `headingAccent` split in `content.js`, 2026-07-17 user request) with the **sub stacked below** the heading (single flex column; the old heading-left/sub-right grid is gone), then `WHY_TEKNIIK.items` as a 4-col row. Each cell has a **2px near-black top border**, a large ghosted Inter Tight index (`01`–`04`, → `--accent` on hover), title, desc; the cell lifts on hover (CSS transform, reduced-motion gated). 4→2 col (≤900px) → 1-col (≤560px). No scroll hooks (simplified from the old aurora spine).

## Process — "Phase horizon" (`07`, 5 phases, inverted band + WebGL dot wave)
`src/sections/Process.jsx` (2026-07-14 revamp) is a full-bleed **brand-navy band** (`--band`, identical in both ink modes) with an animated **three.js dot-wave field** rolling behind the content: `src/components/DottedSurface.jsx` (adapted from 21st.dev "dotted-surface" — recreated in Tekniik idiom: JSX + CSS module + theme tokens, no Tailwind/next-themes). The wrapper IO+`requestIdleCallback`-gates a `lazy()` import of `DottedSurfaceScene.jsx` so `three` ships as its own chunk (~130 KB gz) that only loads near the section; the scene is container-sized (ResizeObserver), DPR-capped at 2, resolves dot/fog colors from `--on-band-dim`/`--band` at mount, pauses its rAF loop when offscreen (IO), and fully disposes on unmount. Reduced motion (or pre-load) renders a static CSS dot texture instead — the band never reads as empty. The layer is edge-faded via CSS mask, `aria-hidden`, `pointer-events: none`.
Content: head row = `07 | HOW WE WORK` meta + oversized Satoshi heading (left) with the sub right-anchored on the same baseline (stacks ≤720px). The **5** `PROCESS.steps` (Listen/Plan/Build/Launch/Support & Grow — split from 4 in the 2026-07-16 spec sync) sit on a **descending staircase** 5-col grid (`margin-top: calc(var(--i) * clamp(...))`, gentler drop for 5); each phase = a 2px top hairline whose **teal fill races across in sequence** with scroll (`useScrollProgressInk`: per-rail `scaleX(clamp(p*total - i, 0, 1))` where `total = steps.length` — order-agnostic, node ignites as its fill starts), mono `PHASE 0X` (accent-on-band) + duration pill, ghost stroke number, Satoshi title, dim desc. The **last phase's rail** ("Support & Grow" · Ongoing) gets an open-ended teal chevron cap (`.step:last-child .rail::after`) so it reads as continuing. Hover lifts the phase −6px + teal ghost stroke (pointer-fine only, no text-shift). ≤960px 2-col zig-zag (even items +36px), ≤600px 1-col. Reduced motion → rails filled, nodes lit, chevron shown, no canvas.

## Portfolio — "Work index" (`08`, `id="work"`)
`src/sections/Portfolio.jsx` renders the 8 `PORTFOLIO` projects as a **bento card grid** under an `08 | OUR WORK` head (redesigned 2026-07-16 from the old editorial list — user preference). The `<section>` carries `id="work"` so the Hero "See our work" CTA can smooth-scroll to it. Layout: 4-col grid, `grid-auto-flow: row dense`, `grid-auto-rows: minmax(208px,auto)`; span classes assigned **by data** via `cellFor(item)` (2026-07-21, user: "the card with no case study smaller"): `featured → cellBig` 2×2, `route → cellWide` 2×1, everything else → `cellSmall` 1×1. Items render **featured → routed → rest** (three filter spreads, not `CELLS` by position) so the mono numbering `01–08` follows the visual order the dense grid produces — don't map `PORTFOLIO.items` in raw order, dense backfill would jumble the numbers. The 14 span units leave the last row half-filled (two smalls, whitespace right) — intentional asymmetric ending, not a bug. CareGrid (`tcc`) is the **navy 2×2 anchor** (`--band`, `desc` shown); the three routed case studies (`looqz`, `famili`, `autoscreen`) get wide tiles with "View case study →" `<Link>`s (others are `<article>`). `.foot` is `flex-wrap: wrap` and `.ctaIcon` is `flex-shrink: 0` (2026-07-21 — the StoryNest small tile flex-crushed the CTA arrow to 0 width; now the CTA wraps to its own line and the card grows instead of squeezing the arrow). Card (`Card` subcomponent): mono index + **region chip with a location-pin glyph** (`LocationGlyph`, the last tag = country), big Satoshi title, **small single-line tag pills** (`.tags` is `nowrap`; small tiles pass `compact` → one pill only, so tags never wrap), hairline foot with `result` + CTA. Hover (pointer-fine, REDESIGNED 2026-07-21 — user called the old stacked effect "AI slop"): **one confident move on every card** — the `.accentLine` rail draws across the top and a faint teal wash (`.card::after`, `linear-gradient(--accent-tint → transparent 70%)`, the site's row-sweep device; `.featured` gets its own brighter rgba wash since the light tint vanishes on navy) fades in beneath it; routed cards add the CTA gap/arrow nudge + a `scale(0.99)` press. The REMOVED pile-up — lift −6px, `--shadow-card-hover`, border-color swap, title underline (`.titleInk` deleted from JSX+CSS), region-pin recolor — must NOT come back. `--bg-raise` cards sit flat on the canvas now. Responsive: 4-col → 2-col (≤1024, anchor stays 2×2, rest square to 1×1) → 1-col (≤640, all spans reset). Reduced-motion kills lift/draws.

## Testimonial — "The word" (LIGHT, rotating)
`src/sections/Testimonial.jsx` is a **light** section (`--bg-2`, top hairline) — kept light ON PURPOSE so it does not merge with the near-black `FinalCta` band directly below it. Asymmetric editorial spread: left rail = oversized `--accent` quote glyph + attribution behind a vertical hairline; right = the quote set huge in display type. **Rotates through the 5 `TESTIMONIALS`** (`content.js` array, one per flagship client): auto-advance every 5s, gated by an IntersectionObserver (only while ≥35% in view), paused on focus-within, and **disabled entirely under reduced motion** (manual nav still swaps instantly). There is deliberately **NO hover pause** — the cursor parks on the section after scrolling and froze the rotation (user bug report); do not re-add one. Transition = outgoing quote+attribution fade up (`.leaving`, 300ms) → keyed remount fades the incoming quote in (`quoteIn` keyframe) and re-enters the attribution via `attrIn` (both `backwards` fill, NOT `both` — forward fill would out-prioritise the `.leaving` styles). The `FadeIn` entrance runs ONLY on the very first scroll-in (`rotated` state flips after the first swap) — replaying it per rotation read as a glitch per user. Section height never jumps: all 5 quotes render as `visibility:hidden` sizers stacked in the same grid cell as the live quote. Nav = thin editorial hairline bars in the rail foot (active = `--accent`), each inside a 44×44 button hit area; global `:focus-visible` ring from reset.css covers them. FinalCta remains the single dark crescendo, so the page rhythm is …light → light Testimonial → dark FinalCta.

## AiAccelerated — "AI-Accelerated Development" (`10`, NEW 2026-07-16)
`src/sections/AiAccelerated.jsx` + `.module.css` — the homepage spec's §10, added between Testimonial and FinalCta. Background is a **navy pre-crescendo wash** (`linear-gradient(180deg, var(--bg), var(--prelude))` + one static teal `.bloom` radial top-right), NOT a flat grey and NOT dark — it deepens top-to-bottom toward the navy FinalCta so it never reads as the same grey as the Testimonial above, while FinalCta stays the single dark crescendo (the page's tuned band rhythm; see MiniCta note). **Full-band AI circuit (`AiCircuit`, 2026-07-20 — replaced the right-side `NeuralNet`; user: "use the entire section's space, not just the right side… microchip and electrons should depict AI development"):** an absolute full-width svg (`viewBox 0 0 1440 520`, `width:100%; height:auto`, bottom linear-mask fade) of faint 45°-step blueprint traces — the home hero's trace language — spanning the whole band and converging on a **microchip** right of the head (rounded body + inner die + mono "AI" label + unconnected pin stubs completing the silhouette; `.chipPulse` breathes). The story is the acceleration: **slow dim electrons drift IN from the left** (`.haloIn`/`.coreIn`, 6.5–12s) and **faster, brighter, doubled-up electrons stream OUT the right** (`.haloOut`/`.coreOut`, 3–4s ×2 per path), SMIL `animateMotion` like the hero. Path ids are `aib-*` so they never collide with the hero's `hc-*` on the same page. `z-index:-1`, hidden ≤900px (needs the header whitespace); electrons + chip pulse stop under reduced motion, static circuit remains. Prior evolution: crude SMIL graph → Hero corner circuit → 3-4-2 NeuralNet → this. Head = `10 | AI-ACCELERATED` meta + `FadeIn` Satoshi heading + sub. Four `AI_ACCELERATED.points` on a 4-up grid (2-col ≤900, 1-col ≤520): each card = small outline glyph badge (inline SVG, `currentColor` so both ink modes recolor), mono `0N` index, title, desc, on a 2px `--border-strong` top border. **Card glyphs are lucide-language** (2026-07-20, user: chevrons/shield/pulse/star read "old fashioned"): braces (engineering) / scan-check (QA) / route (delivery) / shapes (prototyping). **Hover** (pointer-fine, RM-gated): card lifts −6px, a teal 2px rail (`.card::before`, `scaleX` origin-left, 320ms) DRAWS across the top border, and the icon chip fills `--accent` with `color: var(--bg-raise)` (white in light ink, dark navy on the light-teal dark-ink accent — never hardcode white) + `translateY(-2px) scale(1.06) rotate(-4deg)` pop. **Hover-delay gotcha (fixed):** the card box is a **child** of its `Reveal` wrapper (`.cardWrap` holds the reveal + its stagger `transition-delay`; `.card` has its own snappy `cubic-bezier(0.22,1,0.36,1)` transition) — putting `.card` directly on the `Reveal` made the per-card stagger delay leak onto the hover transform (card 4 waited 240ms → "stuck"). Match Portfolio: Reveal wraps, card is the child. Closes on a teal-railed (`border-left: 3px --accent`) Satoshi pull statement (`AI_ACCELERATED.closing`). All color from tokens; reduced-motion kills hover/lift. The `.bloom` is static (no keyframe) so it's RM-safe by default.

## FinalCta — "Crescendo" (inverted)
`src/sections/FinalCta.jsx` (props from `Home.jsx`) is a full-bleed **inverted** closing stage (`--band`) with two background layers: (1) a `.section::before` **teal bloom** — one soft multi-stop radial (small-rgba teal, smooth 5-stop falloff to avoid banding on the dark band) top-right, slowly breathing via `bloomBreathe` (translate3d + scale + opacity, 14s alternate; killed under reduced motion). A 21st.dev "BackgroundPaths" animated-SVG recreation lived here briefly (2026-07-14) but read as a glitch per user — replaced with this pure-CSS bloom; do NOT reintroduce stroke-dash background effects here, and do NOT install Tailwind/framer-motion for pasted 21st.dev/shadcn components — recreate in-idiom. (2) the giant `ChevronWatermark` brand mark (logo chevron paths, faint white + teal fills, bleeding off the bottom-right). Both `heading` lines render as `FadeIn` (simple fade; final line `--accent-on-band`); a magnetic primary `Button` (`useMagneticInk`) and a mono `mailto:` link with a `scaleX` underline-draw close it.

## Footer — office addresses stay; narrative geography doesn't (2026-07-20, refined same day)
**GEOGRAPHY RULE (refined): office ADDRESSES are fine anywhere (footer + contact) — what's banned is narrative copy implying the company/development is UK/Chennai-scoped** (user first said "remove Chennai/UK references", then clarified: "just the references, not the actual address in the footer — revert that"). The Footer's Location column is the original office-tabs treatment (Chennai/London tabs + address + flag + country, `FOOTER.offices`/`OFFICES` shared with Contact; `.country` at Satoshi 700, not 600). Column headings render the label PLAIN — the `// ` mono prefix was removed 2026-07-20 (user: "put it normally"); don't re-add it. `IconFlagIndia`/`IconFlagUK` are user-supplied rounded-badge SVGs (2026-07-20, `viewBox 0 0 32 32`, own 4px corner radius + inset `opacity .15` border baked into the art) — the `.flag` wrappers in Footer/Contact therefore carry NO border-radius/overflow/box-shadow ring (the old square ring is what misaligned them); don't re-add one. Contact's office-tab `.flag` is 16px (12px visible art) with `position: relative; top: -1.5px` — browser-measured so the art overhangs the label's 8px caps by exactly 2px top AND bottom (2026-07-21, second user pass: the 18px flag was center-matched to the line box but still read misaligned because its overhang past the caps was asymmetric — align flags to the CAP band, not the line box). Do NOT re-add copy like "UK team", "based in Chennai/UK", "Agency UK" (index.html title/meta stay de-UK'd), and do NOT add explicit "Global/Remote-first" copy either — global reach is implied, not stated. Client geography (portfolio region chips, case-study copy, testimonial attributions) is exempt — social proof, not company footprint. About signals cell and HeroBuildBoard footer ("One team / Always shipping") stay geography-free. **Brand-column contacts (2026-07-21):** only `hello@tekniik.ai` remains — the HR mail (`hr@tekniik.ai`) and WhatsApp rows were removed per user (Contact page keeps its own HR/WhatsApp lines from `CONTACT` data — footer-only removal). Below the email sits a `.socials` row: LinkedIn/Instagram/X as 34px hairline squares (`--r-md`, teal hover, `scale(0.94)` press, RM-gated) driven by `FOOTER.socials` — hrefs are intentionally empty (render as `'#'`) until the profile URLs land; fill them in `content.js` when available. Icons are `IconLinkedIn`/`IconInstagram`/`IconXSocial` (fill-based brand glyphs in `Icon.jsx`); `IconWhatsApp` was deleted as dead code with the footer row — re-add it only if a WhatsApp icon is needed again.

## Cursor / magnetism
The site uses the system default cursor (no `Cursor` component, no `cursor: none`). `useMagnetic` (`src/motion/`) IS used — it applies subtle pointer pull to the primary CTAs on Hero and FinalCta (reduced-motion gated inside the hook).

## Generated imagery
All under `public/img/`, generated via **Imagen 4 Ultra** (`imagen-4.0-ultra-generate-001`) on the Google Generative Language API, using the `GEMINI_API_KEY` env var. All sources are **square 1:1** so they crop cleanly into both the bento and the page-hero square frames.

`scripts/generateTekniikImages.js` ships **three style boilerplates** because the consumers want different treatments:
- **CINEMATIC** — full-bleed editorial product photography with deep indigo/violet/pink atmospheric haze, glowing rim light, and bokeh particles. Designed for `object-fit: cover` + a dark scrim overlay (used by bento cards + case-study `heroFrame`). The composition fills the 1:1 frame edge-to-edge so cropping any aspect works.
- **DOCUMENTARY** — real-world editorial photography for the page-hero full-bleed images. Magazine-spread aesthetic, 50mm at f/2.0, soft natural daylight with subtle indigo+violet+pink color grading, modern minimal interior. Compositions are **strictly right-weighted** (subject occupies the right 55%, left 45% reserved for text overlay). When `allowPeople: true`, the script switches Imagen's `personGeneration` to `allow_adult` and drops the people exclusions from the negative prompt; faces are still framed off-axis (3/4 back, side profile, soft focus) to avoid AI-face-artifact risk.
- **ABSTRACT** — pure form compositions on a clean near-white surface: translucent geometric volumes, layered curved planes, luminous orbs. **No devices, no UI, no text, no faces** — kept available for the case where a page wants a sculptural still life. Currently unused by page heroes (replaced by DOCUMENTARY) but available for future product-shot work.

Inventory:
- `services/{websites,apps,mobile,ai}.webp` (1600×1600, **cinematic**) — bento + Services-page row visuals. Websites = laptop with a designed marketing site; Web Apps = glassy browser frame with an analytics dashboard; Mobile = two iOS phones with home + detail screens; AI = AI-assisted editor with suggestion bubbles + pipeline graph.
- `page/{services,about,contact}-hero.webp` (1920×1080, **documentary**, 16:9) — formerly PageHeader/About-hero media — ALL now unused (About runs HeroBuildBoard, Contact HeroThread, Services the showreel; PageHeader deleted). Kept on disk as paid generations — see ISSUES.md. Original subjects:
  - **About** ("A small team that builds big things") → three adults working together at a wooden desk in a bright modern studio, focused on a laptop and notebook. Faces angled obliquely (`allowPeople: true` set on this item).
  - **Services** ("We build the technology your business runs on") → a modern developer workstation on a clean wooden desk: open laptop with abstract code panels, mechanical keyboard, notebook + brass pen, ceramic mug, plant.
  - **Contact** ("Let's talk about your project") → a clean modern desk ready for a conversation: smartphone with abstract gradient lock screen, open notebook with brass fountain pen, indigo + warm-pink swatch cards, ceramic mug.
- `case/{looqz,autoscreen}-hero.webp` (1600×1600, **cinematic**) — case-study `heroFrame` media. Looqz = phone with booking flow in front of laptop with web-view marketplace; AutoScreen = phone with quote form in front of laptop with technician map.

Imagen 4 dropped the `negativePrompt` parameter, so exclusions ("no people, no faces, no real brand logos, no Lorem ipsum, no monospace") are folded inline into each prompt body. Both style boilerplates additionally repeat these constraints. Sharp resize is `fit: 'cover'` since both treatments compose centred and bleed naturally; webp encoded at quality 92 with effort 6 + smartSubsample. Re-run with:

```
node scripts/generateTekniikImages.js                # all 9 (Ultra)
node scripts/generateTekniikImages.js --id svc-ai    # one entry
node scripts/generateTekniikImages.js --model std    # standard tier (faster/cheaper)
node scripts/generateTekniikImages.js --dry-run      # preview prompts
```

## Case studies — shared "spec sheet" template (Deep Ink, 2026-07-20)
`src/pages/CaseStudy.jsx` + `CaseStudy.module.css` render all three `/case/<slug>` routes; `CaseLooqz.jsx` / `CaseAutoScreen.jsx` / `CaseFamili.jsx` are thin wrappers that normalise their content shapes (`groups` = the two bullet-ledger blocks, `quotes` array, `flowCols` 3/4, `sceneKey`). This replaced the old indigo PageHeader + white-card design (PageHeader.jsx was DELETED — no route uses it). Template: hero meta bar (node + `CASE STUDY / <TITLE>` | `← All work` → `/`) → split hero (FadeIn title at `--fs-page-hero` + sub left; the discipline's ServiceVignettes scene right on the frameless blueprint stage — GlowBook→`MobileScene`, ScreenFix→`AppScene`, IO-ignited once; the old cinematic `case/*.webp` renders are unused) → **signals ledger** (4 `StatBlock` count-ups; StatBlock now renders ★ via `StarredText`, weight 900) → `01 / THE CHALLENGE` (hairline-railed prose column) → `02 / WHAT WE BUILT` (lede, per-group mono subhead + numbered specRow ledger, flow intro + `--cols`-driven flow cells with 2px `--border-strong` top borders) → `03 / THE RESULT` (prose column, teal-railed Satoshi pull quotes with mono attribution, `Tag` pill row) → FinalCta from the case's `finalCta` copy. All copy verbatim from `CASE_LOOQZ`/`CASE_AUTOSCREEN`.

## Routing
`/` Home · `/services` · `/services/:slug` (ServiceDetail — valid slugs `custom-software` / `web-platforms` / `mobile-apps` / `ai-systems`, unknown slug renders NotFound) · `/about` · `/contact` · `/case/looqz` · `/case/autoscreen` · `/case/famili` (StoryNest) · `/work` · `/support` · `/website-package` · `/privacy-policy` `/terms-of-service` `/cookie-policy` `/gdpr` (all four render the shared `LegalPage` with a `page` prop) · `*` NotFound. `ScrollToTop` resets scroll on route change. Route fade (240ms) keyed by pathname on the wrapper div — the pathname key also remounts ServiceDetail when navigating between sibling services.

## Spec-driven pages (2026-07-20 — built from `content/*.md` locked specs)
- **Legal ×4** — `src/pages/Legal.jsx` (+`Legal.module.css`, data in `src/data/legal.js` `LEGAL_PAGES`): one quiet template — mono `LEGAL` meta bar + "Last updated" stamp, h1 BELOW `--fs-page-hero` (deliberate: clamp(2rem,3.4vw,2.8rem)), 720px reading column, tables in `overflow-x:auto` wrappers. Copy verbatim from `tekniik-legal-pages-spec.md`; company = "Tekniik AI Studio Private Limited" in body only, NO CIN anywhere.
- **`/work`** — `src/pages/Work.jsx`: hero → full-width navy `--band` featured CareGrid card (ghost "01", no CTA — no case page yet) → 2-col grid of the other 7 `PORTFOLIO.items` in the homepage card idiom; items WITH a `route` link "View case study →", the rest show a quiet non-interactive "View project →" label (no dead `#` links). Card hover matches the homepage Portfolio exactly (2026-07-21, user: the old lift+border+shadow+underline pile-up read as AI slop): ONE move — the `.accentLine` rail draws across the top and a `.card::after` teal wash (`--accent-tint` gradient, `z-index: -1` inside the isolated card) bleeds down, plus CTA gap/arrow nudge on routed cards and `:active scale(0.99)` press feedback. No translateY lift, no box-shadow, no title underline (the `.titleInk` span/rules were removed).
- **`/support`** — `src/pages/Support.jsx`: two audiences as separate registers (clients = canvas specRow ledger; rescue = `sectionTint` band, "Inherited a mess?"), 3-up arrangement cells, FinalCta "Get in Touch". No pricing.
- **`/website-package`** — `src/pages/WebsitePackage.jsx`: standalone ad landing (deliberately not linked from Nav or Footer — ads/direct-URL entry only). £599 price in the ink stat-card treatment, specRow inclusions, About-style rail steps on a navy band, native `<details>` FAQ rows, FinalCta with `emailNote`. Renders inside the normal Nav/Footer shell.
- **StoryNest case** — `/case/famili`: `CaseFamili.jsx` wrapper + `CASE_FAMILI` in content.js (transcribed from `references/tekniik-prototype-v4.html`, every "FamiliCloud"→"StoryNest"); `sceneKey="mobile"`, 3 groups (archive/bridges/app), flowCols 3. `StatBlock` now renders fully non-numeric values (`Free`) verbatim via `parsed.raw` — the counter path is numeric-only (was rendering "0Free").

## Service detail pages — shared "spec sheet" template (Deep Ink, 2026-07-17)
`src/pages/ServiceDetail.jsx` + `ServiceDetail.module.css` render all four `/services/<slug>` routes from the `SERVICE_PAGES` map in `content.js` (copy VERBATIM from `content/tekniik-services-spec.md`; each entry: `name`, `headline`, `problem[]`, `deliver[]`, `approach`, `cta`, `sceneKey`, optional `caseStudy`). Template: hero meta bar (node + `SERVICES / <NAME>` eyebrow | `← All services` breadcrumb to the hub) → split hero (FadeIn h1 + static teal .heroRail left, the discipline's ServiceVignettes scene right on a frameless blueprint stage — `SCENES` maps `sceneKey` web/app/mobile/ai; IO-ignited once, `container-type: size`, stacks below text ≤960px) → `01 / THE PROBLEM` (hairline-railed paragraph column) → `02 / WHAT WE DELIVER` (mono-numbered specRow ledger, max-width 780px — the spec requires a list, never a paragraph) → `03 / OUR APPROACH` (teal-railed Satoshi statement) → conditional case-study strip (`--surface` card: kicker/title/desc | hairline-split foot with result + "View case study →"; only web-platforms→ScreenFix `/case/autoscreen` and mobile-apps→GlowBook `/case/looqz`; StoryNest `/case/famili` exists too but isn't wired to a service page) → "Also explore" cross-link rows (the other 3 services, 3-col ≥960) → FinalCta fed the page's `cta` copy. Wire-up: homepage `CAPABILITIES[].to` deep-links each capability, and the hub's disciplines carry a mono "Full details →" `.detailLink` beside their CTA button (`META[].detail` in `Services.jsx`; hub keys map websites→web-platforms, apps→custom-software). Deliberately NO per-page `<title>`/meta — site-wide gap, see ISSUES.md.

## Adding a new portfolio entry
1. Add an item to `PORTFOLIO.items` in `src/data/content.js` with `slug`, `title`, `tags`, `desc`, `stack` (array of tech), `result`, optional `route`, optional `featured: true`.
2. If interactive (case study), add a route in `src/App.jsx` and a thin wrapper page in `src/pages/` that feeds the shared `CaseStudy.jsx` template (content entry in `content.js`, `sceneKey` picking the discipline vignette, `groups`/`quotes`/`flowCols`). Case heroes use coded vignettes, not generated imagery.

## Quality gates
- `npm run build` must succeed
- `npm run lint` must pass (`react-hooks/set-state-in-effect` is an error)
- Visual sweep at 320 / 414 / 768 / 1280 / 1600 viewports — no horizontal scroll
- No console errors with `prefers-reduced-motion: reduce` enabled

## QA harness
`tasks/qa-test.py` is a Playwright script that visits every route at 5 viewports, captures screenshots, and reports console errors + horizontal-scroll regressions. Run with the dev server up:

```
npm run dev               # in another shell
python tasks/qa-test.py
```

Screenshots land in `tasks/shots/` and should be deleted after review.

## Things deliberately omitted (vs. brief)
- No Famili Cloud case study route — brief lists only Looqz and AutoScreen as case studies.
- No CSS framework — vanilla CSS Modules. (An earlier note here said "no animation library"; that is no longer true — the `src/motion/` layer uses framer-motion + Lenis + OGL, see Stack.)
